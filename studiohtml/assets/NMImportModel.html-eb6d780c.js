import{_ as e,a as o,b as d,c,d as t}from"./model_attr_modelid_auto-9f5127b1.js";import{_ as r}from"./store_import_obj_place-8ebde174.js";import{_ as i,p as a,q as s,a1 as l}from"./framework-204010b2.js";const n={},p=l('<h1 id="导入模型" tabindex="-1"><a class="header-anchor" href="#导入模型" aria-hidden="true">#</a> 导入模型</h1><p>本文概述了从使用 <strong>Autodesk Maya</strong> 或 <strong>Blender</strong> 等工具创建的模型，向 <code>MiniStudio</code> 上传资源商城的过程。</p><h2 id="上传资源商城" tabindex="-1"><a class="header-anchor" href="#上传资源商城" aria-hidden="true">#</a> 上传资源商城</h2><p>最简便的模型导入方法是使用 资源商城 窗口的 <strong>‘+’</strong>（导入）按钮（<code>View</code>（视图）选项卡 → 资源商城）。</p><p><img src="'+e+'" alt="store_import_obj"></p><p>完成上传后，将会在个人资源文件目录中显示该上传的模型（如图，上传一个 <code>spring</code>（弹簧）的<code>ojb</code>模型）：</p><p><img src="'+o+'" alt="store_import_obj_success"></p><h2 id="模型使用" tabindex="-1"><a class="header-anchor" href="#模型使用" aria-hidden="true">#</a> 模型使用</h2><h5 id="一、直接从资源商城拖出放置模型" tabindex="-1"><a class="header-anchor" href="#一、直接从资源商城拖出放置模型" aria-hidden="true">#</a> 一、直接从资源商城拖出放置模型</h5><p>鼠标左键选中 <strong>资源商城</strong> 的模型（不松开），拖动至地图区域，松开鼠标左键即自动放置到该位置</p><p><img src="'+r+'" alt="store_import_obj_place"></p><h5 id="二、创建model-修改属性配置成相应模型-弹簧模型" tabindex="-1"><a class="header-anchor" href="#二、创建model-修改属性配置成相应模型-弹簧模型" aria-hidden="true">#</a> 二、创建model，修改属性配置成相应模型（弹簧模型）</h5><ol><li>打开 <strong>场景</strong> 管理窗口（<code>View</code>（视图）选项卡 → 场景） ，在 <code>WorkSpace</code> 中创建一个 <code>Model</code></li></ol><p><img src="'+d+'" alt="workspace_add_model"></p><ol start="2"><li><p>在 <strong>场景</strong> 管理窗口中选择新创建的 <code>Model</code> 对象。</p></li><li><p>在 <strong>属性</strong> 窗口的 <code>Model</code> 属性栏，点击 <code>modelid</code> 属性。修改为资源商城获取的资源<code>id</code>。配置完成后，就能看到 <code>Model</code> 对象已更新为想要设置的模型（弹簧）啦！</p><p>可以通过如下两种方式设置<code>modelid</code>:</p><ul><li>手动输入：通过复制资源商城<code>obj</code>资源<code>id</code>，粘贴到<code>modelid</code>属性输入框</li></ul><p><img src="'+c+'" alt="model_attr_modelid_hand"></p><ul><li>自动设置：点击 <code>modelid</code> 属性右侧输入框，点选 <code>“...”</code> 按钮，在下拉框选择设置方式：<strong>上传资源</strong> 或者 <strong>云资源</strong>。</li></ul><p><img src="'+t+'" alt="model_attr_modelid_auto"></p></li></ol><h2 id="应用纹理" tabindex="-1"><a class="header-anchor" href="#应用纹理" aria-hidden="true">#</a> 应用纹理</h2><p>开发者可以通过设置 <code>Model</code> 的 <code>TextureId</code> 为其应用纹理。此操作适用于 <code>Studio</code> 和脚本运行时。</p><p>若希望在 Studio 环境中应用网格纹理，请遵循以下步骤：</p><ol><li><p>在 <strong>场景</strong> 管理窗口的层次结构中选中需要应用纹理的<code>Model</code>（弹簧）对象。</p></li><li><p>在 <strong>属性</strong> 窗口的 <code>Model</code> 属性栏，单击其 <code>TextureId</code> 区域。修改为资源商城获取的模型图像（弹簧纹理图像）资源<code>id</code>（上传 obj （弹簧）对应的贴图到资源商城，上传成功后拿到该图像的资源id）。配置完成后，就能看到 <code>Model</code> 对象已更新为想要设置的模型（带纹理的弹簧）啦！</p><p>开发者可以通过多种方式设置纹理<code>TextureId</code>:</p><ul><li>当需要通过 <code>MiniStudio</code> 资源 <code>ID</code>（例：<code>sandboxNetworkId&amp;filetype=5://88126516642189312</code>）对纹理进行应用时，可将此 <code>ID</code>通过复制资源商城模型图像（弹簧纹理图像）资源<code>id</code> 粘贴至<code>TextureId</code>右边输入框。</li><li>单击 <code>TextureId</code> 属性右侧输入框，点选 <code>“...”</code> 按钮，在下拉框选择 <strong>云资源</strong>，选择已经上传过的任意图像或纹理。</li><li>单击 <code>TextureId</code> 属性右侧输入框，点选 <code>“...”</code> 按钮，在下拉框选择 <strong>上传资源</strong>，上传新纹理图像。</li></ul></li></ol><h2 id="错误排查" tabindex="-1"><a class="header-anchor" href="#错误排查" aria-hidden="true">#</a> 错误排查</h2><p>当使用网格导入时可能会出现一些问题，其中绝大多数的原因都是没有正确理解 <code>MiniStudio</code> 允许上传的内容类型。下表中列举了部分常见问题与其解决方案概述。</p><table><thead><tr><th>问题</th><th>解答</th></tr></thead><tbody><tr><td>从 <code>Blender</code> 导出的 <code>FBX</code> 文件过大问题</td><td>使用 <code>MiniStudio</code> 美术工具处理</td></tr></tbody></table>',22),h=[p];function _(m,g){return a(),s("div",null,h)}const x=i(n,[["render",_],["__file","NMImportModel.html.vue"]]);export{x as default};
